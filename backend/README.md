# AI Study Backend
The C++ 17 server handling persistence, transcription, and AI orchestration.

## Build Instructions
1. Create build directory: `mkdir build && cd build`
2. Configure: `cmake ..`
3. Build: `cmake --build .`
4. Run: `Debug\ai_notes_server.exe` (Windows) or `./ai_notes_server` (Linux/macOS)
